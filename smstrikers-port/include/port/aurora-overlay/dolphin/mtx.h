#ifndef PORT_AURORA_OVERLAY_MTX_H
#define PORT_AURORA_OVERLAY_MTX_H
// Aurora's own dolphin/mtx.h has no paired-single entry points, so this directory sits ahead of its
// include path and forwards the decomp's header.
#include "../../../dolphin/mtx.h"
#endif
